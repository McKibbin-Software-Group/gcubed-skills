interface:
  display_name: "Skill Capture"
  short_description: "Turn delivery lessons into skill proposals"
  default_prompt: "Use $skill-capture to review recent work, classify whether it needs docs, an existing skill patch, a new skill proposal, or an upstream packet, then follow the route-specific reference before drafting and run the quality gate before presenting or writing candidate files."
